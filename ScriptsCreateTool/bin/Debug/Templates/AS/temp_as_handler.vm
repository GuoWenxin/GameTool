package ${PackageString}.handler{

	import ${PackageString}.message.${MessageName};
	import net.Handler;

	public class $HandlerName extends Handler {
	
		public override function action(): void{
			var msg: ${MessageName} = ${MessageName}(this.message);
			//TODO 添加消息处理
		}
	}
}